'use client';

// Seed button removed - all blogs are now managed through the admin dashboard
// Hardcoded blog files have been deleted as all blogs are stored in MongoDB
export default function SeedBlogsButton() {
  return null;
}
